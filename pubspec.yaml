name: flashcards
description: Flashcards done right.

publish_to: 'none' # DO NOT CHANGE IT

version: '0.0.1'

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod: ^2.3.4
  freezed_annotation: ^2.2.0
  path: ^1.8.2
  sqflite: ^2.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  freezed: ^2.3.2

flutter:
  uses-material-design: true
